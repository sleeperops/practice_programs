# ask user for the first input and store it in input_a
# ask the user for the second input and store it in input_b
# check if both inputs can be converted to an integer
# if not, display 'not numbers'
# If the two values are equal, display 'equal'
# else, display 'not equal'

input_a = input('Input the first number: ')
input_b = input('Input the second number: ')
def compare_ab(a, b):
    if a == b:
        return 'Equal'
try:
    input_a = int(input_a)
    input_b = int(input_b)
    print(f'Answer: {compare_ab(input_a, input_b)}')
except:
    print('>>Not numbers')
# ==============================================



